# MNIST CNN - Нейромережа для розпізнавання цифр

Цей проєкт демонструє створення та навчання **згорткової нейромережі (CNN)** на наборі даних MNIST для розпізнавання рукописних цифр (0-9).

## 📂 Структура проєкту

```
mnist-cnn/
├─ venv/                 ← Віртуальне середовище Python
├─ src/
│  ├─ main.py            ← Скрипт для тренування моделі
│  ├─ model.py           ← Архітектура CNN
├─ mnist_cnn_model.h5    ← Збережена навчена модель
├─ README.md             ← Опис проєкту
├─ requirements.txt      ← Список бібліотек для проєкту
```

## ⚙️ Встановлення та запуск

1. Встановити Python 3.10+ та відкрити VS Code у папці проєкту.

2. Створити та активувати віртуальне середовище:

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1  # для PowerShell
```
Якщо з’являється помилка про Execution Policy, дозволяємо скрипти:

```
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
3. Встановити бібліотеки з requirements.txt:

```
pip install -r requirements.txt
```

Або вручну:

```
pip install tensorflow numpy matplotlib
```

4. Перейти в папку src та запустити нейромережу:

```
cd src
python main.py
```